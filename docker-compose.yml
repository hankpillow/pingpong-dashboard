version: '3'

services:

  nodejs:

    image: node:latest
    working_dir: /var/tmp

    command: ["npm"]

    expose:
      - "8080"

    environment:
      - SSH_AUTH_SOCK=/ssh_agent

    volumes:
      - $SSH_AUTH_SOCK:/ssh_agent
      - ./.git:/var/tmp/.git:rw
      - ./src:/var/tmp
      - ./src/node_modules:/var/tmp/node_modules:rw
